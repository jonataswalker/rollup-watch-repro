{
  "name": "rollup-watch-repro",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rollup -c -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "rollup": "^0.47.6"
  }
}
